# Финальный роект

## Запуск:

- npm ci && npm run dev
- npm run build
- npm run lint

## Чеклист

### Архитектура и структура

- [x] В shared/ui лежат только презентационные (UI) компоненты:
- без бизнес-логики,
- без работы с API,
- без доступа к стору,
- принимают данные и коллбэки только через props.
- [x] Features изолированы в отдельных папках.
- [x] Widgets выделены отдельно
- [x] Выделены entities — user и product
- [x] providers, routing, подключение store в app
- [x] Нет глубоких prop-chain’ов без необходимости.

### Оптимизация рендеров

- [x] Использован Profiler — найден hotspot.
- К примеру, мемоизированы карточки товаров Card из списка CardList, что даёт перерисовку только одной карточки при добавлении в избранное или добавлении её в корзину и не перерисовывается весь список CardList. (Card в React.memo завёрнут, в useProducts из которого достаются products использованы useMemo для query-параметров и значений фильтрации)

### React.Portal для модалок

- [x] В public/index.html добавлен <div id="modal-root"></div>
- [x] Реализован компонент Modal в src/shared/ui/Modal через ReactDOM.createPortal
- [x] Закрытие модального окна через клик по иконке крестика, клику вне области модалки и нажатие на клавишу ESC корректно обрабатываются
- [x] Фокус обрабатывается корректно:
- при открытии модального окна начальный фокус устанавливается на кнопку закрытия модалки (иконку крестика)
- при закрытии модального окна — фокус возвращается на элемент-триггер.

### useRef — реальное применение

- [x] Автофокус на инпут формы регистрации и логина при монтировании через ref
- [x] Для сохранения id таймера setTimeout в компоненте shared/ui/Modal

### Собственные сборки (esbuild/swc)

- [x] Создан альтернативный сборочный конфиг: vite.config.js с SWC (@vitejs/plugin-react-swc)
- [x] Для сравнения характеристик сборок:
- для webpack использовался webpack-bundle-analyzer: 1.79 Mb, время: 18.4s
- для vite rollup-plugin-visualizer: 1.15 Mb, время: 7.3s

### Применение React 19 Hooks

- [x] Использован useOptimistic для добавления отзыва на товар в src/widgets/ReviewList
